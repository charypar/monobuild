name: Go
on: [push]
jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - name: Set up Go 1.13
        uses: actions/setup-go@v1
        with:
          go-version: 1.13
        id: go

      - name: Check out code into the Go module directory
        uses: actions/checkout@v1

      - name: Get dependencies
        run: |
          go get -v -t -d ./...
          if [ -f Gopkg.toml ]; then
              curl https://raw.githubusercontent.com/golang/dep/master/install.sh | sh
              dep ensure
          fi

      - name: Link code into GOPATH
        run: |
          ls -l $(go env GOPATH)/src/github.com/charypar
          mkdir -p $(go env GOPATH)/src/github.com/charypar
          rm -rf $(go env GOPATH)/src/github.com/charypar/monobuild
          ln -s $(pwd) $(go env GOPATH)/src/github.com/charypar/monobuild
          ls -l $(go env GOPATH)/src/github.com/charypar

      - name: Build
        run: |
          ls -l $(go env GOPATH)/src/github.com/charypar
          ls -l .
          ls -l $(go env GOPATH)/src/github.com/charypar/monobuild/
          make

      - name: Test
        run: |
          ls -l $(go env GOPATH)/src/github.com/charypar
          ls -l .
          ls -l $(go env GOPATH)/src/github.com/charypar/monobuild/
          make test
